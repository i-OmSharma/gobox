# ── Build output ─────────────────────────────────────────────────────────────
/veil
bin/
dist/
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out

# ── Go toolchain ──────────────────────────────────────────────────────────────
/vendor/
*.mod.bak

# ── Container runtime artifacts ───────────────────────────────────────────────
# State file written to /var/lib/veil/ at runtime — not part of source
/var/lib/veil/
# Overlay work/upper/merged dirs created per container
overlayfs/*/
# Unpacked image layers and rootfs trees
layers/
images/
*.rootfs/
*.layer.tar

# ── Test & profiling ──────────────────────────────────────────────────────────
coverage.out
*.coverprofile
*.prof

# ── IDE / editor ──────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~

# ── Local tool settings ───────────────────────────────────────────────────────
.claude/

# ── Secrets & environment ─────────────────────────────────────────────────────
.env
.env.*
*.pem
*.key

# ── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Binary ────────────────────────────────────────────────────────────────────────
veil-linux-amd64